import { ObservableLoaderPipe } from './observable-loader.pipe';

describe('ObservableLoaderPipe', () => {
  it('create an instance', () => {
    const pipe = new ObservableLoaderPipe();
    expect(pipe).toBeTruthy();
  });
});
